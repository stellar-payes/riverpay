[package]
name = "stream-hooks"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "21"
stream-core = { path = "../stream-core" }

[dev-dependencies]
soroban-sdk = { version = "21", features = ["testutils"] }
