services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: riverpay
      POSTGRES_PASSWORD: riverpay
      POSTGRES_DB: riverpay
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
